const producto = "monitor 20 pulgadas";
//conoce la cantidad de caracteres
console.log(producto.length);

//para saber si una cadena esta presente en la que estamos trabajando
console.log(producto.includes("monitor"));

