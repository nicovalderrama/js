const autenticado = true;

if (autenticado) {
    console.log('Acceso permitido');
} else {
    console.log('Acceso denegado');
}

//operador ternario
const acceso = autenticado ? 'Acceso permitido' : 'Acceso denegado';
